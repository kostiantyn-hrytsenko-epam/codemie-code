# CodeMie Code Configuration Example
# Copy this file to .env and fill in your values

# ==========================================
# Core Configuration (Required)
# ==========================================
# Choose ONE of the provider configurations below

# Provider: ai-run, bedrock, anthropic, azure
CODEMIE_PROVIDER=ai-run
CODEMIE_BASE_URL=https://ai.run/api/v1         # API endpoint URL
CODEMIE_API_KEY=your-api-key-here              # Your API key
CODEMIE_MODEL=claude-4-5-sonnet                # Model to use

# ==========================================
# Optional Configuration
# ==========================================
CODEMIE_TIMEOUT=300                             # Request timeout in seconds (default: 300)
CODEMIE_DEBUG=false                             # Enable debug logging (default: false)
CODEMIE_MCP_SERVERS=filesystem,cli-mcp-server   # MCP servers to load (comma-separated)

# Security (usually not needed, defaults work well)
# CODEMIE_ALLOWED_DIRS=/path/to/project         # Allowed directories (comma-separated)
# CODEMIE_IGNORE_PATTERNS=node_modules,.git     # Ignore patterns (comma-separated)

# ==========================================
# Configuration Priority
# ==========================================
# 1. CLI arguments (highest)     --model gpt-4
# 2. Environment variables        CODEMIE_MODEL=gpt-4
# 3. Project config               .codemie/config.json
# 4. Global config                ~/.codemie/config.json
# 5. Defaults (lowest)            claude-sonnet-4
#
# Environment variables in .env override project/global config!

# ==========================================
# Provider-Specific Examples
# ==========================================

# Option 1: AI/Run LiteLLM (Recommended - Unified AI Gateway)
# CODEMIE_PROVIDER=ai-run
# CODEMIE_BASE_URL=https://litellm.codemie.example.com
# CODEMIE_API_KEY=your-litellm-api-key
# CODEMIE_MODEL=claude-4-5-sonnet
#
# For direct Claude usage (bypassing codemie):
# ANTHROPIC_BASE_URL=https://litellm.codemie.example.com
# ANTHROPIC_AUTH_TOKEN=your-litellm-api-key
# ANTHROPIC_MODEL=claude-4-5-sonnet
# Note: Use ANTHROPIC_AUTH_TOKEN (not ANTHROPIC_API_KEY) for LiteLLM/AI-Run

# Option 2: AWS Bedrock (Claude via AWS)
# Requires AWS credentials (Access Key ID & Secret Access Key)
#
# Step 1: Configure AWS credentials (choose one method):
#   Method 1: AWS CLI (recommended) - run: aws configure
#   Method 2: Environment variables (below)
#   Method 3: AWS credentials file (~/.aws/credentials)
#
# AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE        # Required: Your AWS Access Key ID
# AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG  # Required: Your AWS Secret Access Key
# AWS_REGION=us-west-2                          # Required: AWS region
# AWS_PROFILE=default                           # Optional: AWS CLI profile name
#
# Step 2: Set Bedrock configuration
# ANTHROPIC_MODEL=us.anthropic.claude-sonnet-4-5-20250929-v1:0
# CLAUDE_CODE_USE_BEDROCK=1
#
# Note: AWS credentials can also be configured via:
#   - AWS CLI: aws configure
#   - Credentials file: ~/.aws/credentials
#   - IAM roles (for EC2/ECS instances)

# Option 3: Anthropic (Direct API)
# CODEMIE_PROVIDER=anthropic
# CODEMIE_BASE_URL=https://api.anthropic.com/v1
# CODEMIE_API_KEY=sk-ant-...
# CODEMIE_MODEL=claude-sonnet-4

# Option 4: Azure OpenAI (for GPT models and Codex)
# CODEMIE_PROVIDER=azure
# CODEMIE_BASE_URL=https://your-resource.openai.azure.com
# CODEMIE_API_KEY=your-azure-api-key
# CODEMIE_MODEL=gpt-4
